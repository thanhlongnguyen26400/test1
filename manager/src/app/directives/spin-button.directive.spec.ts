import { SpinButtonDirective } from './spin-button.directive';

describe('SpinButtonDirective', () => {
  it('should create an instance', () => {
    const directive = new SpinButtonDirective();
    expect(directive).toBeTruthy();
  });
});
