import { ImageConvertPipe } from './image-convert.pipe';

describe('ImageConvertPipe', () => {
  it('create an instance', () => {
    const pipe = new ImageConvertPipe();
    expect(pipe).toBeTruthy();
  });
});
